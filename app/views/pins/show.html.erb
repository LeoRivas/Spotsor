<div class="container">
<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @pin.name %>
</p>
<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>

<p>
  <strong>Photo:</strong>
  <%= image_tag(@pin.photo) %>
</p>
<% if user_signed_in? %>
<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>
</div>

<%= link_to "Mas fotos del usuario", pinsof_path(@pin.user.id) %>